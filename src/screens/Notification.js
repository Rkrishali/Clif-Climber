import { StyleSheet, Text } from "react-native"

const Notification = () => {

    return(
        <Text>
            Notification
        </Text>
    )
}

const styles = StyleSheet.create({

})
export default Notification